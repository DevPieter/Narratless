plugins {
    id 'net.fabricmc.fabric-loom' version '1.17-SNAPSHOT' apply false
    id 'com.replaymod.preprocess' version '221276c7d4'
}

preprocess {
    def mc262 = createNode('26.2', 262, null)
    def mc261 = createNode('26.1', 261, null)

    mc262.link(mc261, null)
}